import React from 'react';
import {Text} from 'react-native';

const Onboard = (props) => {
    return(
        <>
        <Text>This is the Onboard Page</Text>
        </>
    );
};

export default Onboard;